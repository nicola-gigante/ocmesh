#
# Lines starting with a 'sharp' sign are comments
#

# We can assign names to primitives and reuse them

object obj1 = translate({100, 200, 100}, sphere(100))
object obj2 = translate({100, 100, 100}, cube(200))

object result = unite(obj2, obj1)

# Materials have to be declared
material metal

# mycup is declared as one of the final objects of the scene,
# so we must set the material
build metal result
